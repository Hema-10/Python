def are_rotations(str1, str2):
    if len(str1) != len(str2):
        return False

    return (str2 in str1 + str1)print(are_rotations("abcd", "cdab"))  # Output: True
print(are_rotations("abcd", "acbd"))  # Output: False
print(are_rotations("abcd", "cdab"))  # Output: True
